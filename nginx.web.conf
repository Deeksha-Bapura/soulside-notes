server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: any unmatched path serves index.html so client-side
    # routing (React Router etc.) works on hard refresh / direct navigation.
    location / {
        try_files $uri $uri/ /index.html;
    }
}